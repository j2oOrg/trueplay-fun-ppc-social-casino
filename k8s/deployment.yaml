apiVersion: apps/v1
kind: Deployment
metadata:
  name: trueplay-fun-ppc-social
  labels:
    app: trueplay-fun-ppc-social
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trueplay-fun-ppc-social
  template:
    metadata:
      labels:
        app: trueplay-fun-ppc-social
    spec:
      containers:
        - name: trueplay-fun-ppc-social
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: trueplay-fun-ppc-social
  labels:
    app: trueplay-fun-ppc-social
spec:
  type: LoadBalancer
  selector:
    app: trueplay-fun-ppc-social
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  loadBalancerIP: LOAD_BALANCER_IP_PLACEHOLDER